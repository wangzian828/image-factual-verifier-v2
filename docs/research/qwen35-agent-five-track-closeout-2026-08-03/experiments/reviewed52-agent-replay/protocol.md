# H5 protocol: reviewed-52 deterministic Agent replay

Status: locked before execution.

## Frozen mechanism set

Use `scripts/replay_snapshot_discrepancy.py` with the historical reviewed-52
scoring release. Do not rerun retrieval.

Core confirmatory cases:

- Maid Marian: relation positive (`on` versus `beside`);
- L'Hoest: fine-grained visible property;
- surgery tray: controlled negative;
- Diana: source-pixel contradiction and Gemini visual-failure guard;
- mummy support alignment and Seagram ambiguity controls.

The broader batch includes every reviewed-52 snapshot that has qualified,
task-owned source Evidence before a Decision checkpoint.

## Required fixes and metrics

1. source-visible property must be concise and pixel-checkable;
2. resolved pixel Evidence must be consumed or explicitly proven irrelevant;
3. focused-visual failure/exhaustion must remain an engineering failure and must
   never become source-only support;
4. no false composite on controls;
5. report trigger precision, visual Evidence creation, post-visual consumption,
   composite success, terminal verdict, and engineering failure by provider.

## Acceptance

Core cases must satisfy their locked expected mechanism. Broader-case changes must
not regress existing reducer, replay, prompt-boundary, or strict trace tests.
